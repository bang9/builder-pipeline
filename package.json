{
  "name": "builder-pipeline",
  "version": "0.0.1",
  "private": true,
  "description": "All-in-one toolkit for solo builders — automate everything except the actual building.",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
